/**
 * Clase encargada de realizar operaciones sobre una imágen 
 * */
class HDN {
    /**
    * Crea una copia de la imagen proporcionada.
    * @param {img}Objeto de la clase imagen
    */
    constructor(img) {
        this.img = img;
    }
  
    /**
     * 
     * @param {}
     * @return {} 
     */
    metodo() {
    }
}