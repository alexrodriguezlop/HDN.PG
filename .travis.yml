# Arrancamos nuestro docker, la orden RUN implica la orden PULL.
script:
  - docker run -t -v `pwd`:/test alexrodriguezlop/hdn.pg
# Configuramos las noticaciones.
# Denegamos es envío de email
notifications:
  email:
    recipients:
      - alexrodriguezlop@gmail.com
    on_success: never 
    on_failure: never 
