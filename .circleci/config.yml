version: 2.1
orbs:
  node: circleci/node@3.0.0
workflows:
  node-tests:
    jobs:
     - run:
          name: Instalación de paquetes
          command: |
            npm install -g gulp-cli |
            npm install
      - run:
          name: Test
          command: gulp test

